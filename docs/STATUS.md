# playtesthub — Build Status

Phase tracker. Concise by design — each phase is a one-line outcome that points at the canonical doc for the *what* and the *how*. PRD changes go in [`CHANGELOG.md`](CHANGELOG.md); requirements in [`PRD.md`](PRD.md); schemas in [`schema.md`](schema.md); error rows in [`errors.md`](errors.md); build conventions (repo layout, test strategy, smoke harness, CI gates) in [`engineering.md`](engineering.md); CLI surface in [`cli.md`](cli.md); DM mechanics in [`dm-queue.md`](dm-queue.md); AGS retry/cleanup in [`ags-failure-modes.md`](ags-failure-modes.md); operator runbooks in [`runbooks/`](runbooks/).

## Status legend

- `not started` — no code written yet
- `in progress` — some code landed; milestone not yet complete
- `shipped` — milestone deliverables merged and demoed

## Milestones

Milestone scope is defined in PRD §10.

| Milestone | Scope (summary) | Status |
| --------- | --------------- | ------ |
| **M1 — Backend + admin CRUD + signup (STEAM_KEYS only)** | Postgres schema (Playtest incl. `distributionModel`/`agsItemId`/`agsCampaignId`/`initialCodeQuantity`, Applicant, Code, `leader_lease`). Admin Portal extension site with Playtest list + create/edit (+ soft-delete). Player Svelte app: landing + Discord-through-IAM login + signup form with `config.json` loader. `distributionModel` field exists but only `STEAM_KEYS` is functional. Golden flow stops at "applicant is PENDING". | shipped — phase 13 (player a11y + admin codegen-fresh CI gate) deferred as M3 polish |
| **M2 — NDA + approval + code grant (pool-only) + AGS Campaign API integration** | NDA click-accept + versioned acceptance storage. Admin applicants page with approve/reject. Key pool management: STEAM_KEYS CSV upload; AGS_CAMPAIGN pool status + "Generate more codes" + "Sync from AGS". AGS Campaign API integration (Item + Campaign creation + code generation/fetch). Approve API: reserve → fenced finalize (pool-only grant, §4.1 step 6). Background reclaim job with TTL. `AuditLog` write path wired for NDA edits, playtest edits, `code.grant_orphaned`, `code.upload_rejected`, and `campaign.*` events. Golden flow stops at "player sees code in UI". | shipped — phases 1–16 + 8.1 done. |
| **M3 — Survey (when configured) + Discord notifications + polish/docs/demo** | Survey builder (text / rating / multi-choice, optional per playtest). Survey response submission (one-shot immutable) + responses viewer (aggregates split by `surveyId`). Discord DM notification on approval. Audit log viewer UI (paginated, filterable, JSON diff). Perf proof point: 500 signups / 10 min at p95 < 3s end-to-end. README walkthrough (including RBAC warning), demo video, public MIT repo. | not started |

---

## Phase tracker

Phase status legend: `[ ]` not started · `[~]` in progress · `[x]` shipped.

Each shipped phase carried tests + smoke-harness extension + verification per the TDD + smoke rule in [`engineering.md`](engineering.md) §4–5. The *what* of each phase lives in the canonical tech doc cited; this file tracks *did it land* and surfaces decisions/notes that don't fit anywhere else.

### M1 — Backend + admin CRUD + signup (STEAM_KEYS only)

Target: golden flow stops at "applicant is PENDING".

1. `[x]` **Scaffold from template** — clone `AccelByte/extend-service-extension-go`, reinit fresh repo, rewrite module path to `github.com/anggorodewanto/playtesthub`, drop CloudSave `pkg/storage`, add `migrations/` + `pkg/repo/` + `pkg/config/`. See [`engineering.md`](engineering.md) §1.1.
2. `[x]` **Proto `playtesthub.v1` (M1 RPCs)** — 10 M1 RPCs from PRD §4.7 with `buf lint` clean. Wire surface frozen here; HTTP annotations drive the OpenAPI spec the admin codegen consumes.
3. `[x]` **Postgres schema — migration 0001** — `Playtest`, `Applicant`, `Code`, `leader_lease`, `AuditLog` per [`schema.md`](schema.md). `pkg/migrate` runner + testcontainers test land alongside.
4. `[x]` **Repository layer (`pkg/repo`)** — one package member per table (interface + Postgres impl). Integration tests against testcontainers-postgres cover slug uniqueness across soft-delete, `UNIQUE (playtestId, value)` on Code, audit JSONB round-trip, status-transition CAS.
5. `[x]` **Config + IAM wiring** — `pkg/config` env-var parser (PRD §5.9 vars); `pkg/iam` JWT validator wrapping the AccelByte Go SDK; AGS admin `sub` → `actorUserId` plumbed through request context.
6. `[x]` **Playtest RPCs — service layer** — STEAM_KEYS-only `CreatePlaytest` (AGS_CAMPAIGN returned `Unimplemented` until M2 phase 8); `EditPlaytest` whitelist; `SoftDeletePlaytest`; `TransitionPlaytestStatus`; the three `Get` variants; `ListPlaytests`. Every [`errors.md`](errors.md) row asserted in handler tests.
7. `[x]` **Signup RPC** — `Signup` + `GetApplicantStatus`. Discord handle lookup via bot token (`pkg/discord`), fallback to raw Discord ID on failure (PRD §10 M1). Idempotent on `(playtestId, userId)`.
8. `[x]` **Extend App UI skeleton** — `admin/` scaffolded off `AccelByte/extend-app-ui-templates@templates/react`. Vitest + RTL added (template ships no runner). Playtest list + create/edit/delete pages against generated react-query hooks. Cloud deploy validated end-to-end on ISC namespace `abtestdewa-pong`.
   - **Permission workaround**: admin RPCs declare `ADMIN:NAMESPACE:{namespace}:EXTEND:APPUI` instead of the semantically correct `CUSTOM:ADMIN:NAMESPACE:{namespace}:PLAYTEST` because AGS Admin Portal in ISC does not yet let game admins assign `CUSTOM:*` perms. Documented in [`engineering.md`](engineering.md) §8 with revert plan.
   - **Cookie-bearer fallback**: `pkg/common/authServerInterceptor.go` accepts the AGS Admin Portal's `access_token` cookie as a bearer-token fallback. The Admin Portal ships its session in an httpOnly cookie, never as `Authorization:` — confirmed as the intended Extend App UI auth signal.
   - **AppUI distribution caveat**: Internal Shared Cloud only (PRD §9 R11). The dev `extend-helper-cli` binary on Google Drive carries the `appui` subcommand; public v0.0.10 release does not.
8.1. `[x]` **Phase 1–8 review fixes** — batch fix for spec-drift before phase 9: stop leaking NDA text to logs (drop `PayloadReceived`/`PayloadSent` from `WithLogOnEvents`), close the soft-cap bypass (count soft-deleted rows), fix `EditPlaytest` partial-update semantics (don't clobber un-set fields, only re-hash NDA when text changes), extend `cloud.sh` to phase 7/8 RPCs, reject `NdaRequired=true` + empty `NdaText`, lock `EditPlaytest` immutable-field contract via reflective test, replace hand-rolled cookie parser with `net/http`, reject `initial_code_quantity` on STEAM_KEYS. Each lands a test + an [`errors.md`](errors.md) row where applicable.
9. `[x]` **Player Svelte app skeleton** — `player/` with Svelte 5 + Vite + TS + Tailwind v4. `src/lib/config.ts` hard-fails on every PRD §5.8 malformed branch. Hash routing for static-host compat. Vitest covers config validation, PKCE (RFC 7636 known vector), token exchange, API wrappers, router, and all five routes. NDA accept / code retrieval / survey deferred to M2/M3.
9.1. `[x]` **Discord-through-IAM URL shape + IAM client wiring** — verified URL shapes against ISC: `/iam/v3/oauth/authorize` (not `/iam/v3/authorize`); `idp_hint=discord` is silently ignored. Public PKCE-only IAM client registered. Path-based `/callback` (AGS rejects fragments) + `bridgePathCallback()` rewriting `/callback?...` → `/#/callback?...` for the hash-router. Manual end-to-end smoke blocked → 9.2.
9.2. `[~]` **Self-hosted Discord login (proxy RPC, AGS-mediated `/oauth/authorize` flow)** — superseded by 9.3. Code shipped; live token exchange failed with `invalid_grant: justice platform account not found`. Root cause: AGS `LoadAuthorize` requires a Justice platform account that the auth-code grant never creates in game namespaces. No client-side workaround — see [`engineering.md`](engineering.md) §7 "Why the auth-code flow doesn't work" for the source-traced explanation. All 9.2 code (`Player.GetDiscordLoginUrl` proxy RPC, `pkg/service/discord_login.go`, `PLAYER_IAM_CLIENT_ID` env var, `fetchDiscordLoginUrl` helper) was removed in 9.3.
9.3. `[x]` **Discord federation via platform-token grant** — switched to AGS's `POST /iam/v3/oauth/platforms/discord/token`. Player runs Discord OAuth directly; backend exchanges the Discord code for AGS tokens via confidential `AGS_IAM_CLIENT_ID`/`SECRET`. AGS auto-creates the Justice platform account on first call. New `Player.ExchangeDiscordCode` RPC. Architecture + flow documented in [`engineering.md`](engineering.md) §7 "Discord federation via platform-token grant"; error rows in [`errors.md`](errors.md).
9.4. `[x]` **Phase 9 e2e re-verification** — golden flow green end-to-end on `abtestdewa-pong`. Player → Discord OAuth → `/v1/player/discord/exchange` → 200 + valid AGS token → signup → `status=PENDING`. Three surprises landed alongside, all documented in [`runbooks/discord-login.md`](runbooks/discord-login.md): (a) AGS wraps Discord 4xx `invalid_grant` as HTTP 500 `error=server_error` — handler detects the marker and surfaces `InvalidArgument` ([`errors.md`](errors.md) row added); (b) AGS Admin Portal's Discord `RedirectUri` MUST byte-equal `${PLAYER_ORIGIN}/callback` (AGS ignores the form-body `redirect_uri`); (c) `${PLAYER_ORIGIN}/callback` must be on the Discord developer portal allowlist.
9.5. `[x]` **Game-developer setup runbook** — [`runbooks/setup-ags-discord.md`](runbooks/setup-ags-discord.md). Prescriptive walkthrough: Discord developer portal → AGS Admin Portal Discord platform → IAM client → playtesthub config → verification ladder → "Three URLs that MUST agree byte-for-byte" → common failure modes with the byte-exact error strings to grep for. Companion to the descriptive [`runbooks/discord-login.md`](runbooks/discord-login.md).
10. `[x]` **CLI harness (`pth`)** — see [`cli.md`](cli.md). Sliced into 6 sub-phases; each shipped its own smoke probes + tests + describe-catalogue entry.
10.1. `[x]` **Skeleton + meta + first unauth RPC** — `pth version` / `doctor` / `playtest get-public --slug` + `--dry-run` plumbing. Sets the precedence/exit-code/output patterns reused by every later subcommand ([`cli.md`](cli.md) §4 + §8).
10.2. `[x]` **Auth (password) + credentials store** — `pth auth login --password` (ROPC), `logout`, `whoami`, `token`. `~/.config/playtesthub/credentials.json` (0600/0700 perms enforced); refresh-token rotation on 60s leeway; token resolution `--token > PTH_TOKEN > store` keyed on `(addr, namespace, profile)` ([`cli.md`](cli.md) §7.2–7.3). Bootstrap config (`PTH_AGS_BASE_URL` / `PTH_IAM_CLIENT_ID` / `PTH_IAM_CLIENT_SECRET`) is env-only — not a global flag.
10.3. `[x]` **Discord-federated login (loopback + manual)** — `pth auth login --discord [--manual] [--no-browser]` mirrors the player's platform-token-grant flow (not AGS authorize-URL). Fixed loopback port `14565` because Discord's allowlist is byte-exact ([`cli.md`](cli.md) §7.1). Setup constraint: register `http://127.0.0.1:14565/callback` on Discord *and* AGS Admin Portal Discord `RedirectUri` ([`runbooks/setup-ags-discord.md`](runbooks/setup-ags-discord.md) § "CLI loopback origin"). One-Discord-credential-per-namespace trap noted there too.
10.4. `[x]` **User group (AGS IAM admin endpoints)** — `pth user create / delete --yes / login-as`. `create` uses `POST /iam/v4/admin/namespaces/{ns}/test_users` (AGS-generated username/password/email; marks the row as a test user). The native non-federated user has no Discord ID claim, so backend Signup falls back to the raw IAM `sub` per PRD §10 M1 — that's the path the e2e suite uses ([`cli.md`](cli.md) §6.1 user group).
10.5. `[x]` **Playtest + applicant subcommands** — every M1 single-RPC wrapper from [`cli.md`](cli.md) §6.1. `edit` rejects immutable-field flags client-side with a tailored error (e.g. `slug is immutable (PRD §5.1) — create a new playtest instead`) before any flag-parse runs. Applicant subcommands take `--slug` (cli.md updated to match).
10.6. `[x]` **`describe` catalogue + `flow golden-m1` + CI diff-check** — `pth describe` emits stable JSON catalogue (`cli-schema.v1`) sourced from `cmd/pth/registry.go`. `pth flow golden-m1 --slug <s>` composes create-playtest → transition OPEN → signup → assert PENDING (NDJSON one line per step, stops at first failure). Golden file `cmd/pth/testdata/describe.golden.json` diff-checked in CI.
11. `[x]` **E2E golden flow (M1 stop)** — `e2e/golden_m1_test.go` boots backend in-process via `internal/bootapp` against testcontainers-postgres on a free port, builds `pth` into a tempdir, isolates `PTH_CREDENTIALS_FILE`, drives admin login → user create → user login-as → `pth flow golden-m1`, parses the 4-line NDJSON, asserts `status=OK` per step. Gated on six `AGS_*` / `E2E_*` env vars; skips cleanly without secrets so CI stays green. Bootapp pattern documented in [`engineering.md`](engineering.md) §2.
12. `[x]` **CI + README walkthrough** — `.github/workflows/ci.yml` six parallel jobs (go-lint, go-test, proto-lint, proto-fresh, player, admin); README "Reproduce the golden flow" walks `pth flow golden-m1` end-to-end; `LICENSE` at repo root.
   - **Spec deviations** (both pushed to phase 13): admin job swapped `cg:download` for `cg:clean-and-generate` (the canonical URL is on a private ISC host the runner can't reach); Svelte a11y gate (`@axe-core/playwright`) intentionally absent because no Playwright scaffold exists yet.
13. `[ ]` **Player a11y gate + admin codegen-fresh gate** — two [`engineering.md`](engineering.md) §5 gates that phase 12 left unwired. Both are M3 polish (PRD §10 M3 README walkthrough cadence), not M1 golden-flow correctness — no behavioral risk in shipping M1 without them.
   - **Player a11y**: wire `@axe-core/playwright` over the five player pages with zero-critical-violation assertion scoped to `wcag2a, wcag2aa, wcag21a, wcag21aa`.
   - **Admin codegen-fresh**: both `admin/swaggers/` and `admin/src/playtesthubapi/` are gitignored — the job has no usable prerequisites. Three viable rewires: (a) commit the generated client tree, (b) commit `admin/swaggers/playtesthub.json` and run `cg:clean-and-generate` in CI, (c) repoint `admin/swaggers.json` at the proto-derived `gateway/apidocs/api.swagger.json` (already CI-deterministic via `proto-fresh`). Option (c) is cleanest.

### M2 — NDA + approval + code grant + AGS Campaign API + RetryDM

Target: golden flow stops at "player sees code in UI" (PRD §4.1 step 7). 10 new RPCs: `AcceptNDA`, `ListApplicants`, `ApproveApplicant`, `RejectApplicant`, `RetryDM`, `GetGrantedCode`, `UploadCodes`, `TopUpCodes`, `SyncFromAGS`, `GetCodePool`.

1. `[x]` **Proto `playtesthub.v1` M2 RPCs** — 10 RPCs above declared per PRD §4.7 with full grpc-gateway HTTP annotations + admin permission options matching M1 phase 8. New entities: `NDAAcceptance`, `Code`, `CodePoolStats`, `CodeState` enum. All return embedded `Unimplemented` until each phase implements it.
2. `[x]` **Migration 0002 — `NDAAcceptance` table** — composite PK `(user_id, playtest_id, nda_version_hash)` per [`schema.md`](schema.md). `audit_log.action` is plain TEXT with no CHECK constraint — the M2 action set is enforced via per-action JSONB round-trip tests rather than a DB enum.
3. `[x]` **Repository extensions** — `pkg/repo/nda.go` (`AcceptIdempotent` + `Get` + `LatestForApplicant`); applicant CAS for status/DM (`ApproveCAS`, `RejectCAS`, `UpdateDMStatus`); code pool methods (`Reserve`, `FencedFinalize`, `Reclaim`, `BulkInsertCSV`, `BulkInsertGenerated`, `ListByPlaytest`); typed audit writers in `auditlog_m2.go` (one per M2 action, JSONB shape per [`schema.md`](schema.md)). The fenced finalize integration test asserts the canonical `UPDATE` from [`schema.md`](schema.md) §"Approve flow" returns 0 rows on a reclaim-and-steal between reserve and finalize.
4. `[x]` **`AcceptNDA` RPC + re-accept gating** — handler with idempotent natural-key replay (replay returns the original row, no audit). NDA hash is taken from the live playtest server-side, never client-supplied. `M1 EditPlaytest` audit backfill landed here too (`nda.edit` row only when text actually changed). `NdaReacceptRequired` is client-derived per PRD §5.3 — backend just exposes `currentNdaVersionHash` on the playtest + `ndaVersionHash` on the applicant. Player Svelte: NDA accept route at `/nda`.
5. `[x]` **STEAM_KEYS code upload + `GetCodePool`** — `UploadCodes` enforces PRD §4.3 (UTF-8, charset `[A-Za-z0-9._-]`, length 1–128, file ≤10 MB, ≤50,000 codes, file-level + cross-row dedup, **whole-file reject**) under `pg_advisory_xact_lock(hashtext(playtestId::text))`. `code.upload` payload is `{count, sha256, filename}` only — explicit redaction-canary test catches a future `value` field regression. `GetCodePool` returns counts + raw values per PRD §5.7 admin-exempt rule. STEAM_KEYS-only; AGS_CAMPAIGN returns `FailedPrecondition`.
6. `[x]` **Approve / Reject + reclaim worker + leader lease** — `ApproveApplicant` runs `Reserve → FencedFinalize → ApproveCAS` inside one tx via `repo.TxRunner` ([`engineering.md`](engineering.md) §2). Empty pool maps to model-specific `ResourceExhausted` ([`errors.md`](errors.md) rows 12–13); fenced 0-row writes `code.grant_orphaned` outside the rolled-back tx and returns `Aborted "reservation expired, please retry"` (row 10). `RejectApplicant` is terminal CAS. `ListApplicants` cursor pagination (default 50, max 200, opaque base64 token) + status + dm_failed filters. `GetGrantedCode` (player) hides soft-deleted as `NotFound`. Reclaim worker lives in `internal/reclaim/`; emits `{event:"reclaim_tick", released, leaseHolder}` INFO. Concurrency integration test (`pkg/repo/approve_concurrency_test.go`) races two goroutines on the same applicant — exactly one winner.
7. `[x]` **DM queue (`pkg/dmqueue`) + `RetryDM`** — full mechanics in [`dm-queue.md`](dm-queue.md): bounded FIFO (`DM_QUEUE_MAX_DEPTH`=10k), drain rate (`DM_DRAIN_RATE_PER_SEC`=5), restart sweep (re-marks only `last_dm_status IS NULL OR 'pending'`; preserves prior `failed` reason), circuit breaker (50 failures / 60s → 5 min pause / auto-resume), four `lastDmError` reasons. **Manual-only `applicant.dm_sent` audit rule**: only RetryDM-driven sends emit it; auto-sends from `ApproveApplicant` deliberately don't ([`schema.md`](schema.md) audit-action `applicant.dm_sent`). Production wires a no-op `Sender` — real Discord DM client lands in M3.
   - **Open follow-up**: `Applicant.discord_handle` carries the human display name when handle lookup succeeds, so DM delivery to that recipient string fails. Closing the snowflake-storage gap is a future migration; queue contract unchanged.
8. `[x]` **AGS Platform client (`pkg/ags`) + AGS_CAMPAIGN auto-provision (sub-caps 1–4)** — `pkg/ags` wraps Item create + Campaign create + CreateCodes + FetchCodes + Item/Campaign delete. Retry policies + cleanup matrix per [`ags-failure-modes.md`](ags-failure-modes.md). `CreatePlaytest` AGS_CAMPAIGN branch wraps a 300s timeout (initial-create exception) around: validate `initial_code_quantity ∈ [1, 50_000]` → create Item → create Campaign (cleanup Item on fail) → batched code generation (`AGS_CODE_BATCH_SIZE`=1000 chunks with `WithoutRetries`) → one `txRunner.InTx` commits playtest row + every code row atomically. Partial fulfillment commits the smaller pool with a WARN log per [`ags-failure-modes.md`](ags-failure-modes.md). Bootapp wires `ags.NewMemClient` so the boot path + smoke + e2e exercise the full code path without an outbound AGS call — phase 8.1 swaps in the SDK adapter.
8.1. `[x]` **SDK-backed AGS adapter** — `pkg/ags/sdk.go` wraps `accelbyte-go-sdk` Platform `ItemService` + `CampaignService` behind the existing `ags.Client` interface. SDK call mapping: `CreateItem` → `Item.CreateItemShort` (CODE-type, DURABLE, non-listable in the `AGS_STORE_ID` store with `/playtesthub` category); `CreateCampaign` → `Campaign.CreateCampaignShort` (REDEMPTION/ITEM, MaxRedeemCountPerCode=1); `CreateCodes` → `Campaign.CreateCodesShort` (which only returns `NumCreated`) followed by `Campaign.QueryCodesShort` filtered by a unique `pth-<hex>` batch name to recover the values; `FetchCodes` → paginated QueryCodes (`Limit=100`); `DeleteItem` → `Item.DeleteItemShort` with `Force=true`; `DeleteCampaign` → `Campaign.UpdateCampaignShort` setting `Status=INACTIVE` (AGS exposes no DELETE — `GetCampaignShort` first to read the required `Name`). 404 on cleanup paths is treated as success. SDK errors are unwrapped to a `*sdkError` carrying the upstream HTTP status (regex over the typed-response `[METHOD /path][NNN]` format) so `RetryPolicy.Run` keeps classifying 429 → `ErrRateLimited`, 4xx → `*ClientError`, 5xx/timeout → `ErrUnavailable`. Bootapp gates wiring on `cfg.AuthEnabled && cfg.AGSStoreID != ""` (new `AGS_STORE_ID` env var); without that, MemClient stays in place so dev / e2e / smoke remain offline. `Server.PlatformLogin` exposes a closure that runs `LoginClient` against the same `TokenRepository` the platform services consume — main.go calls it after `bootapp.New`. Smoke harness asserts the boot-time wiring branch log line so a regression flipping the gate fails fast. Sub-cap 1–7 go/no-go validation against a live ISC namespace remains an operator step — set `AGS_STORE_ID` on the deployment and exercise `pth flow golden-m2 --distribution-model=AGS_CAMPAIGN`.
9. `[x]` **`TopUpCodes` + `SyncFromAGS` (sub-caps 6/7)** — `TopUpCodes`: not idempotent, generate-only, no cross-batch tx (each batch a separate `code.InsertGeneratedAtomic` call so a mid-loop AGS failure preserves prior batches per PRD §4.6 step 4). `SyncFromAGS`: fetch-only recovery, idempotent on `UNIQUE (playtestId, value)`. Both use `ags.DefaultRetryPolicy()` (30s/4 attempts/exponential backoff per [`ags-failure-modes.md`](ags-failure-modes.md)), which is distinct from `CreatePlaytest`'s 300s/no-retry initial-create exception. Cut-if-behind (hide top-up/sync in admin UI) not exercised — both shipped in phase 10.
10. `[x]` **Admin UI — applicants page + key pool page** — Extend App UI under `admin/`. Applicants page: status + DM-failed filters; per-row Approve/Reject confirms; Retry-DM only when `APPROVED && lastDmStatus=DM_STATUS_FAILED`; rejection-reason TextArea (cap 500). Key pool page: STEAM_KEYS upload with rejection list (caps display at 50 + tail count); AGS_CAMPAIGN Generate-more / Sync-from-AGS controls; raw values displayed (PRD §5.7 admin-exempt). Codegen seeded from `gateway/apidocs/api.swagger.json` so `admin-build.sh` runs without a deployed namespace ([`engineering.md`](engineering.md) §5.1).
   - **AppUI cloud-deploy gotchas** documented in [`engineering.md`](engineering.md) §7 + [`admin/README.md`](../admin/README.md) "Two-step build + upload": `BASE_URL` must be set at build time (Vite bakes it into `mf-manifest.json` as `publicPath`); asset host is the **parent namespace**, not the game namespace (the dev CLI's "Asset Base URL" log line lies); browser-DevTools is the only authoritative deploy check (Bearer-token `curl` lies on both directions).
11. `[x]` **Player Svelte — NDA accept + code retrieval views** — NDA accept route landed in M2 phase 4; this phase wires the approved-state code-retrieval surface into `Pending.svelte`. `fetchGrantedCode` against `GET /v1/player/playtests/{id}/grantedCode`. Model-specific redeem instructions (STEAM_KEYS: "Activate a Product on Steam"; AGS_CAMPAIGN: `PublicRedeemCode`). PRD §5.3 §4.1 step 7 fix: APPROVED + re-accept-required keeps the code visible and shows a yellow `role="status"` banner (was previously bouncing to NDA route and hiding the code). axe-core deferred to phase 13.
12. `[x]` **`pth` CLI M2 subcommands + `pth flow golden-m2`** — every [`cli.md`](cli.md) §6.2 entry registered in `cmd/pth/registry.go`. `pth flow golden-m2` extends `golden-m1`: create → transition OPEN → signup → accept-nda → upload-codes → approve → get-code (NDJSON, stop-on-first-failure). Default upload synthesises a `GOLDEN-M2-<SLUG>-NNNN` body so callers don't need to stage a CSV; `--codes-file <path|->` overrides. `describe` golden file regenerated; CI diff-check covers the new entries.
13. `[x]` **E2E golden flow (M2 stop)** — `e2e/golden_m2_test.go` reuses M1's `suiteHarness` (no harness duplication); drives admin → user → `pth flow golden-m2`; asserts seven NDJSON `status=OK` lines + non-empty granted code + `distribution_model=DISTRIBUTION_MODEL_STEAM_KEYS`. AGS_CAMPAIGN sub-test still deferred — the SDK adapter from phase 8.1 needs operator-driven validation against a live ISC namespace (no testcontainer for AGS).
14. `[x]` **CI + README walkthrough (M2 cadence)** — README "Reproduce the golden flow" now drives `pth flow golden-m2` (seven NDJSON lines, `--codes-file <path>` for real CSVs). CI surface unchanged from M1 phase 12 — `e2e/` was already in scope. Phase 13 player a11y stays deferred to M3 polish.
15. `[x]` **Cloud smoke — extend to M2 RPC surface** — `scripts/smoke/cloud.sh` now probes all 10 M2 RPCs (`AcceptNDA`, `GetGrantedCode`, `ListApplicants`, `Approve`/`Reject`/`RetryDM`, `UploadCodes`, `TopUpCodes`, `SyncFromAGS`, `GetCodePool`) as a parameterised array — each unauth request must return `401` so a missing route or regressed auth interceptor fails the deploy gate instead of slipping through silently. No proto/migration churn (coverage-only); validated PASS against deployed `abtestdewa-pong`.
16. `[x]` **AGS namespace bootstrap on first AGS_CAMPAIGN create** — `pkg/ags.Client.Bootstrap` runs once per process before the first AGS_CAMPAIGN `CreatePlaytest` and lazily ensures the store + `/playtesthub` category + a VIRTUAL currency exist; each step treats HTTP 409 as success. STEAM_KEYS playtests skip bootstrap entirely. `AGS_STORE_ID` and `AGS_REGION_CURRENCY_CODE` are now optional — Bootstrap discovers/creates and caches resolved values on the SDKClient. Bootstrap failure surfaces as a mapped gRPC status on the in-flight CreatePlaytest and is retried on the next attempt (transient failures don't wedge the process). See [`engineering.md`](engineering.md) "AGS namespace prerequisites (auto-bootstrapped, M2 phase 16)".

#### Cross-cutting rules

- **Smoke harness lands with the code that introduces it** ([CLAUDE.md](../CLAUDE.md), [`engineering.md`](engineering.md) §5.1).
- **Mid-phase fixup phases** (cf. M1 8.1, 9.1–9.5) reserved as needed; not pre-planned.

### M3 — Survey + Discord notifications + polish/docs/demo

Target: golden flow ends at "player submits survey response" (PRD §4.1 step 8). 7 new RPCs: `CreateSurvey`, `EditSurvey`, `GetSurvey`, `SubmitSurveyResponse`, `ListSurveyResponses`, `ListAuditLog`, `RetryFailedDms`.

1. `[x]` **Proto `playtesthub.v1` M3 RPCs** — 7 RPCs declared (`CreateSurvey`, `EditSurvey`, `GetSurvey`, `SubmitSurveyResponse`, `ListSurveyResponses`, `ListAuditLog`, `RetryFailedDms`) per PRD §4.7 with full grpc-gateway HTTP annotations + admin permission options matching M2 phase 1. New entities: `Survey`, `SurveyResponse`, `SurveyQuestion` (typed via `SurveyQuestionType` enum + optional `MultiChoiceOption` array per [`schema.md`](schema.md) §"Survey entity spec"), `SurveyAnswer` oneof (text / rating / multi_choice), `AuditLogEntry`. All return embedded `Unimplemented` until each phase implements it. Service-descriptor guard test extended to 28 methods.
2. `[x]` **Migration 0003 — `Survey` + `SurveyResponse` tables + repo extensions** — schemas per [`schema.md`](schema.md) §"Survey entity spec": `Survey` (`id`, `playtestId`, `version`, `questions JSONB`, `createdAt`) with `UNIQUE (playtestId, version)`; `SurveyResponse` (`id`, `playtestId`, `userId`, `surveyId`, `answers JSONB`, `submittedAt`) with `UNIQUE (playtestId, userId)` enforcing PRD §5.6 one-shot across version bumps. `pkg/repo/survey.go` exposes `Create` / `EditAsNewVersion` (advisory-locked previous-version read + bump) / `GetCurrent` / `GetByID` / `SubmitOnce` (idempotent return-existing-row on conflict) / `GetByPlaytestUser` / `ListResponses` (cursor on `(submittedAt, id)` DESC + optional `surveyId` filter). `pkg/repo/auditlog.go` adds `List(AuditLogPageQuery)` with `(createdAt, id)` cursor + `actor`/`action` filters (`actorFilter='system'` → `actor_user_id IS NULL` per PRD §4.7). JSONB round-trip tests cover question + answer shape variants; cursor pagination + filter composition pinned via integration tests.
3. `[x]` **Survey CRUD RPCs (`CreateSurvey` / `EditSurvey` / `GetSurvey`)** — admin `Create` is natural-key on `playtestId` (`AlreadyExists` on second call); `Edit` bumps version with question + option UUID preservation (independent of question-id preservation, so moving an option between questions still aggregates the same histogram bucket per [`schema.md`](schema.md)); player `GetSurvey` resolves through `Playtest.surveyId` and inherits `GetPlaytestForPlayer` visibility (DRAFT, soft-deleted, CLOSED-without-approval all map to `NotFound`). Audit writers (`pkg/repo/auditlog_m3.go`) emit `survey.create` `{surveyId, questionCount}` (system) and `survey.edit` `{surveyId, questions}` before/after pairs (admin actor). [`errors.md`](errors.md) gained six rows for the new bounds (≤50 questions, prompt ≤1,000 chars, multi-choice 2–20 options + `label` ≤200 chars). Smoke harness extended in the same PR — `cloud.sh` probes the three new HTTP routes for 401, `pth.sh` exercises `pth survey create/edit/get` dry-run + catalogue presence; `pth survey {create|edit} --from <json|->` and `pth survey get --playtest <id>` ship in [`cmd/pth/survey.go`](../cmd/pth/survey.go) (YAML wrapper deferred to phase 12 per [`cli.md`](cli.md) §6.3).
   - **Spec deviation noted**: STATUS phase-3 prose mentioned "text answer max 4,000 chars" + "rating fixed 1–5" — both are submit-time gates and land with `SubmitSurveyResponse` (phase 4). Phase 3 server-side bounds cover the persisted shape only (question count, prompt length, multi-choice option count + label length).
4. `[x]` **`SubmitSurveyResponse` RPC** — player one-shot landed in [`pkg/service/survey.go`](../pkg/service/survey.go). Replay returns `AlreadyExists` with **nil response body** (the gRPC status carries no leak of the original answers — verified by the redaction-canary test). Gating: applicant must exist + be APPROVED + NDA hash equal to `playtest.currentNdaVersionHash` when NDA is required (server-checked). Mid-fill version race honoured — a `surveyId` from any prior version is accepted as long as it belongs to the playtest. Per-answer validation enforces text ≤ 4,000 chars, rating ∈ [1,5], multi-choice option ids known to the question + single-select questions reject many entries; required questions without an answer yield `InvalidArgument`. CLOSED playtests still accept submits for APPROVED applicants. [`errors.md`](errors.md) gains six rows; [`pkg/repo/survey.go`](../pkg/repo/survey.go)'s `SubmitOnce` natural-key contract proven via the fake store + handler tests. Smoke harness: `cloud.sh` adds the `:submit` 401 probe; `pth.sh` adds `pth survey submit --dry-run` (subcommand wired in [`cmd/pth/survey.go`](../cmd/pth/survey.go) with `--from <json|->`; YAML deferred to phase 12).
5. `[x]` **`ListSurveyResponses` RPC** — admin cursor pagination on `(submittedAt, id)` DESC, DESC landed in [`pkg/service/survey.go`](../pkg/service/survey.go) on top of M3 phase 2's `repo.SurveyResponseStore.ListResponses`. Default 50 / max 200 (capped repo-side via `ListPagedDefaultLimit` / `ListPagedMaxLimit`); malformed `page_token` → `InvalidArgument` (mirrors `ListApplicants`); soft-deleted playtest → `NotFound`. Optional `survey_id_filter` narrows to a single Survey version (PRD §5.6 cross-version aggregate split); empty → every version. Smoke harness extended in the same PR — `cloud.sh` adds the `survey/responses` 401 probe; `pth.sh` adds `pth survey responses --dry-run`. CLI subcommand `pth survey responses --playtest <id> [--survey <sid>] [--cursor <c>] [--page-size N]` ships in [`cmd/pth/survey.go`](../cmd/pth/survey.go) ([`cli.md`](cli.md) §6.3). No new [`errors.md`](errors.md) rows — every condition reuses pre-existing implementation-defined `InvalidArgument` semantics.
6. `[x]` **`ListAuditLog` RPC** — admin cursor pagination on `(createdAt, id)` DESC, DESC landed in [`pkg/service/auditlog.go`](../pkg/service/auditlog.go) on top of M3 phase 2's `repo.AuditLogStore.List`. Default 50 / max 200 (capped repo-side via `ListPagedDefaultLimit` / `ListPagedMaxLimit`); malformed `page_token` → `InvalidArgument` (mirrors `ListSurveyResponses`); soft-deleted playtest → `NotFound`. `actor_filter` accepts `system` (rows where `actor_user_id IS NULL`) or a UUID — anything else is `InvalidArgument`. `action_filter` is exact-match. `before_json` / `after_json` ride out as opaque JSON strings the client renders the diff over (PRD §5.7 page 5). Smoke harness extended in the same PR — `cloud.sh` adds the `auditLog` 401 probe; `pth.sh` adds `pth audit list --dry-run`. CLI subcommand `pth audit list --playtest <id> [--actor <a>] [--action <a>] [--cursor <c>] [--page-size N]` ships in [`cmd/pth/audit.go`](../cmd/pth/audit.go) ([`cli.md`](cli.md) §6.3). Two new [`errors.md`](errors.md) rows for the malformed `page_token` / non-`system`-non-uuid `actor_filter` paths.
7. `[x]` **Real Discord DM client (`pkg/discord` Sender impl) + Migration 0004 snowflake storage** — `*discord.BotClient.SendDM` lands in [`pkg/discord/discord.go`](../pkg/discord/discord.go): `POST /users/@me/channels` → `POST /channels/{id}/messages` with the bot token; 429 surfaces `Retry-After`, 5xx the upstream status, other non-2xx the trimmed body — repo truncates persisted reasons to 500 UTF-8-safe bytes. Migration 0004 adds `applicant.discord_user_id` (nullable TEXT) per [`schema.md`](schema.md); `Signup` ([`pkg/service/applicant.go`](../pkg/service/applicant.go)) stamps it from the IAM `platform_user_id` claim. The DM queue ([`pkg/dmqueue/dmqueue.go`](../pkg/dmqueue/dmqueue.go)) short-circuits NULL/empty recipients to `lastDmError='missing_recipient'` without invoking Discord — documented under "Missing recipient" in [`dm-queue.md`](dm-queue.md). [`internal/bootapp/bootapp.go`](../internal/bootapp/bootapp.go) wires `*BotClient` as `dmqueue.Sender` when `DISCORD_BOT_TOKEN` is set; otherwise falls back to the noop sender so the audit/marking pipeline still exercises end-to-end. Manual-only `applicant.dm_sent` audit rule from M2 phase 7 preserved.
   - **Spec deviation noted**: the phase blurb called for a new [`errors.md`](errors.md) row for `missing_recipient`. `errors.md` is the byte-exact gRPC error reference (PRD §4.7); `lastDmError` reasons are stored-field values, not gRPC codes. The catalogue entry landed under "Missing recipient" in [`dm-queue.md`](dm-queue.md) to keep the docs honest about scope. [`errors.md`](errors.md) gains nothing for this phase.
8. `[x]` **`RetryFailedDms` RPC + admin DM-failed bulk surface** — admin bulk handler landed in [`pkg/service/retry_dm.go`](../pkg/service/retry_dm.go) on top of the new `repo.ApplicantStore.ListDMFailedByPlaytest` (only APPROVED rows whose `last_dm_status='failed'`, newest-first, single non-paginated read). Each row enqueues a `manual=true` `dmqueue.Job` through the same path as `RetryDM`; queue-cap overflow surfaces synchronously inside `Enqueue` (writes `lastDmError='dm_queue_overflow'` + audit) and bumps the response `overflow` counter, so the response is `{enqueued, overflow}` per PRD §5.5. Not idempotent (back-to-back calls enqueue twice for still-failed rows). Soft-deleted / missing playtest → `NotFound`; bad UUID → `InvalidArgument`. CLI: [`cmd/pth/applicant.go`](../cmd/pth/applicant.go) gains `pth applicant retry-failed-dms --playtest <id>` (cli.md §6.3); `cmd/pth/registry.go` lists it under M3, `describe.golden.json` regenerated. Smoke: `cloud.sh` adds the M3 `:retryFailedDms` 401 probe; `pth.sh` adds the `--dry-run` body assertion + describe-coverage entry. No new [`errors.md`](errors.md) rows — every condition reuses existing implementation-defined gRPC semantics.
9. `[x]` **Player Svelte — survey form route** — `/survey` route landed in [`player/src/routes/Survey.svelte`](../player/src/routes/Survey.svelte) wired through [`player/src/lib/router.ts`](../player/src/lib/router.ts) + [`App.svelte`](../player/src/App.svelte). Client-side gating bounces every non-`APPLICANT_STATUS_APPROVED` applicant, every NDA-reaccept-required applicant, and every playtest with `surveyId` unset back to `/pending` (server re-checks). `GetSurvey` 404 also routes to `/pending`. Typed widgets: text → `<textarea maxlength=4000>`; rating → 5 radios bound to `ratingAnswers[q.id]`; multi-choice → checkboxes when `allowMultiple`, radios otherwise. `AlreadyExists` (HTTP 409) renders the same "thanks — your response is recorded" view as a fresh success, with no submitted-answer echo (the gateway returns no body). Mid-fill version-bump detection: a 30-second `setInterval` re-fetches the survey while the form is open; when `version` exceeds the loaded one, a yellow `role="status"` banner shows and `data-testid="survey-version-bumped"` flips on — submit still pins to the originally loaded `surveyId` per PRD §5.6. New API helpers `fetchSurvey` + `submitSurveyResponse` and the `Survey` / `SurveyQuestion` / `SurveyAnswerInput` wire types live in [`player/src/lib/api.ts`](../player/src/lib/api.ts). Vitest suite ([`player/tests/Survey.test.ts`](../player/tests/Survey.test.ts)) pins all three gates, the typed widget render, single-select multi-choice rendering as radios, required-text validation blocking submit, the typed-oneof submit body shape, the 409-thanks path, the 401-bounce, and the version-bump banner with submit-against-loaded-id assertion (11 cases). Router test gains `#/playtest/<slug>/survey`. Smoke (`make smoke` + `npm run test && npm run build`) green. axe-core gating still folded into phase 15.
10. `[x]` **Admin UI — survey builder + responses viewer** — Survey builder page (`/playtests/:id/survey`) and responses viewer (`/playtests/:id/survey/responses`) landed in [`admin/src/federated-element.tsx`](../admin/src/federated-element.tsx). Builder: typed-question add (text / rating / multi-choice via Select), `required` checkbox, up/down reorder (drag deferred — PRD §10 cut-if-behind), multi-choice option editor (2–20 entries with `label` ≤200 chars + `allowMultiple` toggle), `version` displayed read-only ("current version v2 (saving creates v3)"); save dispatches `CreateSurvey` for the first version and `EditSurvey` for every subsequent — preloading via the player `GetSurvey` hook so question + multi-choice option ids round-trip and histogram buckets stay stable. DRAFT playtests with `surveyId` set surface a warning Alert because player `GetSurvey` 404s for DRAFT (proto exposes no admin GET surface; documented in the warning copy). Responses viewer: cursor-paginated tables grouped by `surveyId` column with the current version tagged; per-question aggregates render rating histograms keyed on score 1–5 and multi-choice histograms keyed on `option.id` (stable across version bumps per [`schema.md`](schema.md)); text answers stay row-level (no bulk render — PRD §6 redaction); `surveyIdFilter` Select narrows aggregates to a single version. CSV export still deferred (PRD §10 cut-if-behind). Codegen now seeds from `gateway/apidocs/api.swagger.json` (M1 phase 13 option (c)) — the upstream ISC-hosted swagger URL is no longer needed because the local file is already CI-deterministic via `proto-fresh`.
    - **Admin GetSurvey deviation noted**: STATUS phase 10 prose called for "save calls `Create` for the first version, `Edit` for every subsequent" without specifying preload semantics. Proto exposes only player-side `GetSurvey` (`/v1/player/.../survey`), and that handler returns NotFound for DRAFT playtests. Admin builder reuses the player hook for preload, which works for OPEN/CLOSED but not for DRAFT-with-existing-survey — covered by an in-page warning Alert and a fallback blank form. Adding an admin-side `GetSurvey` RPC is a future hardening if first-version edit on a DRAFT playtest with content becomes a real workflow (today, surveys typically land before first transition to OPEN).
11. `[x]` **Admin UI — audit log viewer page** — `/playtests/:id/audit` route in [`admin/src/federated-element.tsx`](../admin/src/federated-element.tsx). Read-only paginated table (Time / Actor / Action) on top of `usePlaytesthubServiceAdminApi_GetAuditLog_ByPlaytestId`; system rows render the actor cell as a “system” Tag, admin rows show the actor UUID in `<Typography.Text code>`. Filters: actor Select (All / System / Admin user…) and action Select (allowClear, options sourced from the M1+M2+M3 audit-action catalogue mirrored as `AUDIT_ACTIONS` against [`schema.md`](schema.md)). Cursor pagination tracks a `pageStack[]` of previous tokens so Previous/Next stay in lock-step with the server's `next_page_token`; any filter change resets the stack. Per-row expand renders `AuditDiff` — homegrown shallow per-key JSON diff (zero new deps; PRD §5.7 says "pick a small one and pin", inlined avoided the dep) with side-by-side pretty-printed `before` / `after` `<pre>` blocks and a "Changed keys: …" Tag row. Audit payloads in [`schema.md`](schema.md) are flat one-level objects so a shallow walker covers every documented shape; nested values render as embedded JSON inside the changed-key row. No edit/delete affordances. Vitest covers header + filters render, system-vs-admin actor cell render, action-filter / actor=system query plumbing, the type-then-Enter commit boundary on the actor user-id input (no per-keystroke query), expanded-row diff with the changed-key Tag, and Next/Prev pagination cursor flow (7 cases — total admin suite 35).
    - **Spec deviation noted**: PRD §5.7 prose suggested "pick a small library and pin" for the diff renderer. We inlined a 30-line shallow walker rather than pull a new npm dep (e.g. `jsondiffpatch` ~100 KB) — every `AuditLog.before/after` payload in [`schema.md`](schema.md) is a flat 1-level record, so deep-diff value-add is zero today. If a future audit action lands a nested payload, swap the inline `shallowDiff` for a library at that point.
    - **Open follow-up**: PlaytestsListPage actions row now carries six buttons per row (Edit / Applicants / Codes / Survey / Responses / Audit + status-conditional Publish/Close + Delete). Density is fine at 1080p but cramps on narrower viewports; an overflow `Dropdown` is the natural fix when phase 15's a11y audit lands. Captured here, not separately filed.
12. `[x]` **`pth` CLI M3 subcommands + `pth flow golden-m3`** — every [`cli.md`](cli.md) §6.3 entry was already registered (phases 3-6 + phase 8). The remaining deliverable, the `pth flow golden-m3` composite, landed in [`cmd/pth/flow.go`](../cmd/pth/flow.go): ten NDJSON lines extending `golden-m2` with `create-survey → submit-response → list-responses`. The flow seeds a TEXT + RATING question pair inline (no `--from` path needed in the harness), threads the server-assigned `Survey.id` and per-question UUIDs from `CreateSurveyResponse` into the `SubmitSurveyResponseRequest`, and asserts the final `ListSurveyResponses` returns a non-empty `responses[]` (post-condition emits a synthesised FAILED line if empty so the caller's exit code goes red). `--dry-run` renders all ten request shapes with placeholders for the resolved-after-create-survey ids. `flow golden-m3` registry entry added; `describe.golden.json` regenerated; smoke harness extended with the `flow golden-m3 --dry-run` ten-line probe. Spec deviation: STATUS prose mentioned `--from <yaml|->` for survey wrappers — `survey create/edit/submit` accept JSON (per phase 3 spec deviation already noted there), and the YAML wrapper is still deferred. **Two-step deviation note**: STATUS phase 12 also said "`survey create / edit / submit` take `--from <yaml|->`" — those wrappers landed earlier in M3 phases 3 + 4 with JSON support per the deviation already logged on phase 3.
13. `[ ]` **E2E golden flow (M3 stop)** — `e2e/golden_m3_test.go` reuses the `suiteHarness` from M1/M2; drives admin → user → `pth flow golden-m3`; asserts ten NDJSON `status=OK` lines + non-empty granted code + non-zero `responses[]` length on the final list step. DM Sender stays the no-op fake in-process (the real Discord client lives behind `DISCORD_BOT_TOKEN`; e2e covers contract, not delivery). AGS_CAMPAIGN sub-test still operator-driven — no testcontainer for Discord either.
14. `[ ]` **Cloud smoke — extend to M3 RPC surface** — `scripts/smoke/cloud.sh` probes all 7 M3 RPCs (`CreateSurvey`, `EditSurvey`, `GetSurvey`, `SubmitSurveyResponse`, `ListSurveyResponses`, `ListAuditLog`, `RetryFailedDms`) as new entries in the parameterised array — each unauth request must return `401` so a missing route or regressed auth interceptor fails the deploy gate (mirrors M2 phase 15). Coverage-only; no proto/migration churn.
15. `[ ]` **Player a11y + admin codegen-fresh CI gates** — closes the deferral logged in M1 phase 13. Player a11y: wire `@axe-core/playwright` over the six player pages (landing, signup, NDA accept, code view, survey form, post-submit thanks) with zero-critical-violation assertion scoped to `wcag2a, wcag2aa, wcag21a, wcag21aa` per PRD §6 Accessibility. Admin codegen-fresh: option (c) from M1 phase 13 — repoint `admin/swaggers.json` at `gateway/apidocs/api.swagger.json` (already CI-deterministic via `proto-fresh`), then `npm run codegen && git diff --exit-code` in `admin/` per [`engineering.md`](engineering.md) §5 catches a backend HTTP-annotation change the admin UI hasn't regenerated against.
16. `[ ]` **Loadtest harness + perf proof point** — `scripts/loadtest/` per PRD §6 / §7: 500 signups in 10 min against a single-replica deploy with p95 < 3s end-to-end. Reported in [`CHANGELOG.md`](CHANGELOG.md) per release. **Not a CI gate** ([`engineering.md`](engineering.md) §5). Recommended driver: `k6` or `vegeta`; deliverable is the script + a one-shot Markdown report committed under `scripts/loadtest/results/`.
17. `[ ]` **README walkthrough + demo video + MIT public release** — README "Reproduce the golden flow" updated to drive `pth flow golden-m3` (ten NDJSON lines); RBAC warning added per PRD §10 M3 ("any authenticated AGS admin session is authorized — not production safe; see PRD §9 R8"). Demo video ≤5 min showing the golden flow against the maintainer's sandbox AGS namespace per PRD §10 Acceptance. Public MIT release: flip repo visibility, verify `LICENSE` at root, tag `v1.0.0`, draft GitHub release notes pointing at the demo + the four canonical docs.

---

## How to update

When a phase merges, flip its `[ ]` / `[~]` / `[x]` flag and replace its body with a one-line outcome that points at the canonical doc for the *what* and the *how*. Surfacing decisions, deviations, or open follow-ups that don't fit any tech doc is fine — keep them tight (one bullet, named so a future reader can grep). Do not paste full implementation prose here; that belongs in [`engineering.md`](engineering.md), [`schema.md`](schema.md), [`errors.md`](errors.md), or the relevant runbook.

PRD version history goes in [`CHANGELOG.md`](CHANGELOG.md), not here.

### Cut-if-behind tracking

Features that may be cut from their target milestone if the team is behind. Recorded here so the PRD stays aspirational while the build plan tracks scope risk. On cut, record the decision in the milestone row's notes column above.

#### M1
- **Markdown rendering** — superseded: PRD §5.1 specifies plain-text rendering for `Playtest.description` in MVP. Markdown is on the post-MVP backlog (PRD §10 Deferred). Not an active cut candidate.

#### M2
- **AGS_CAMPAIGN top-up UI** — superseded: phases 9 + 10 shipped both controls. Cut not exercised.
- **Rejection reasons** — superseded: phase 10 shipped the optional `rejectionReason` Modal. Cut not exercised.

#### M3
- **Responses CSV export** — ship responses viewer without export if behind. On PRD §10 Deferred backlog.
- **Drag-reorder for survey questions** — ship up/down buttons only if behind.
- **Webhook-channel fallback** — DM-only delivery if behind. On PRD §10 Deferred backlog as R2 mitigation.
